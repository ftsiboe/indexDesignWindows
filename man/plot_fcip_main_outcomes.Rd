% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_helpers.R
\name{plot_fcip_main_outcomes}
\alias{plot_fcip_main_outcomes}
\title{Plot FCIP Main Outcomes (Faceted Bar Charts)}
\usage{
plot_fcip_main_outcomes(
  data,
  colume_outcome,
  colume_year,
  colume_grouping = NULL,
  time_scale_theme = NULL,
  general_theme = NULL,
  palette = c("#003524", "#00583D", "#A0BD78", "#BED73B", "#D7E5C8", "#FEF389",
    "#FFC425", "#BE5E27", "#9DD9F7", "#51ABA0", "#0F374B")
)
}
\arguments{
\item{data}{A \code{data.frame} (or \code{data.table}) with at least the columns:
\code{value}, the column named by \code{colume_year}, the column named by
\code{colume_outcome}, and (optionally) the column named by \code{colume_grouping}.}

\item{colume_outcome}{Character scalar. Name of the column in \code{data} that
defines facet panels (e.g., outcome labels such as
\code{"(A) Net reported acres"} and \code{"(B) Liability amount in U.S. dollars"}).}

\item{colume_year}{Character scalar. Name of the year column in \code{data}
(x-axis, typically the FCIP commodity year).}

\item{colume_grouping}{Character scalar or \code{NULL}. Name of the grouping
column in \code{data} used for fill/color and legend entries. If \code{NULL}, all
bars are treated as a single group.}

\item{time_scale_theme}{Optional \code{ggplot2} scale/theme element controlling the
x-axis (e.g., \code{scale_x_continuous(...)}). If \code{NULL}, a default scale is
applied using the unique values of the supplied year column.}

\item{general_theme}{Optional \code{ggplot2} theme applied to the figure. If
\code{NULL}, \code{ers_theme()} is used and further tweaked (text sizes, legend
layout, facet strips). Requires that \code{ers_theme()} is available in scope.}

\item{palette}{Optional character vector of HEX colors used by
\code{scale_fill_manual()}. If provided, it is applied to the grouping legend.}
}
\value{
A \code{ggplot} object.
}
\description{
The function expects \code{data} to contain:
\itemize{
\item a numeric/factor year column (specified via \code{colume_year});
\item a numeric \code{value} column (bar heights);
\item a categorical "outcome" column (specified via \code{colume_outcome}) used for
faceting; and
\item an optional grouping column (specified via \code{colume_grouping}) used to color/
fill bars and determine legend entries.
}

For legend ordering, the function ranks groups using the subset of rows from
the most recent year \emph{within the facet whose label equals}
\code{"(B) Liability amount in U.S. dollars"}. If your outcome labels differ, you
may want to harmonize them or adjust the code that builds \code{labs}.
}
\details{
Creates a faceted bar chart of FCIP outcomes over time by a chosen grouping,
with a shared x-axis (commodity year) and a legend. Facets are determined by
an "outcome" column in \code{data} (e.g., liability vs. acres), allowing one or
more panels depending on the unique values present.

Internally, the function:
\enumerate{
\item Copies the columns named by \code{colume_outcome}, \code{colume_grouping}, and
\code{colume_year} into standard placeholders used in aesthetics.
\item Builds a ranking of groups from the latest year within the facet whose
label equals \code{"(B) Liability amount in U.S. dollars"}, then maps that
ranking to legend order.
\item Draws a bar chart of \code{value ~ year}, colored/filled by group, and
\code{facet_wrap()}s by outcome (free y-scales).
\item Applies the supplied or default x-axis scale and theme, plus optional
manual fill palette.
}
}
\note{
The parameter names use \strong{\verb{colume_*}} (typo preserved for backward
compatibility). Consider aliasing/renaming in a future version.
}
