% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_turning_point.R
\name{select_tolerance_by_post_stability}
\alias{select_tolerance_by_post_stability}
\title{Select a Data-Driven Tolerance Using Post-Turning-Point Stability}
\usage{
select_tolerance_by_post_stability(
  data,
  outcome,
  group_var,
  x_var,
  benchmark_x = NULL,
  tolerance_grid = seq(0.01, 0.15, by = 0.005),
  consecutive_years = 5,
  penalty_weight = 0.01,
  require_consecutive_x = TRUE
)
}
\arguments{
\item{data}{A data.frame or data.table containing the outcome variable,
grouping variable, and historical-window variable.}

\item{outcome}{Character string. Name of the outcome variable used to evaluate
stability. Examples include \code{"actuarial_index"}, \code{"alternative_lr"}, or
\code{"baseline_lr"}.}

\item{group_var}{Character string. Name of the grouping variable. Separate
tolerance values and turning points are selected for each group.}

\item{x_var}{Character string. Name of the numeric historical-window variable.
For example, \code{"history_years"}.}

\item{benchmark_x}{Numeric. The x-value used as the long-history benchmark in
\code{estimate_turning_point_benchmark()}. If \code{NULL}, the maximum observed value
of \code{x_var} is used.}

\item{tolerance_grid}{Numeric vector. Candidate tolerance shares to evaluate.
The default evaluates values from 1 percent to 15 percent in increments of
0.5 percentage points.}

\item{consecutive_years}{Integer. Number of consecutive historical-window
values that must remain within the benchmark-distance tolerance before the
first x-value is classified as the turning point. The default is \code{5}.}

\item{penalty_weight}{Numeric. Penalty applied to later turning points in the
score function. Larger values favor earlier turning points, while smaller
values allow later turning points if they produce lower
post-turning-point variability. The default is \code{0.01}.}

\item{require_consecutive_x}{Logical. If \code{TRUE}, the function requires the
selected sequence to be consecutive in terms of the actual \code{x_var} values.
This argument is passed to \code{estimate_turning_point_benchmark()}.}
}
\value{
A data.table with one row per group. The output includes the selected
tolerance share, estimated turning point, post-turning-point mean, standard
deviation, coefficient of variation, range, scaled turning point, and the
minimized score.
}
\description{
This function selects a tolerance value for the benchmark-distance
turning-point method by evaluating a grid of candidate tolerance values. For
each tolerance, it first estimates the turning point using
\code{estimate_turning_point_benchmark()}. It then measures how stable the outcome
is after the estimated turning point.
}
\details{
The selected tolerance is the one that minimizes a normalized stability score:

\deqn{
  score = post\_cv + penalty\_weight \times scaled\_turning\_point
}

where \code{post_cv} is the post-turning-point coefficient of variation and
\code{scaled_turning_point} is the estimated turning point divided by the benchmark
x-value. This normalization makes the score more comparable across outcomes
with different units or scales.

This function is designed to make the tolerance parameter in the benchmark
turning-point method more data-driven. Instead of imposing a fixed tolerance,
such as 5 percent, the function searches over a grid of possible tolerances.

For each candidate tolerance:
\enumerate{
\item A turning point is estimated using \code{estimate_turning_point_benchmark()}.
\item The data are restricted to observations at or after the estimated turning
point.
\item The post-turning-point mean, standard deviation, coefficient of variation,
and range of the outcome are calculated.
\item A normalized score is computed using post-turning-point variability and a
penalty for later turning points.
}

The tolerance with the lowest score is selected for each group.

If no valid turning point is found for a candidate tolerance, that candidate
does not contribute to the post-turning-point stability calculation for that
group.
}
