% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/precipitation_trend_analysis_panel.R
\name{estimate_one_state_fgls}
\alias{estimate_one_state_fgls}
\title{Estimate mean and variance trends for one state using an FGLS-style workflow}
\usage{
estimate_one_state_fgls(dt, formula, model = "within", eps = 0.00000001)
}
\arguments{
\item{dt}{A \code{data.table} or data frame containing the state-level panel data.
The data must include \code{panelid}, \code{commodity_year}, \code{precipitation},
\code{trend}, and \code{county_fips}.}

\item{formula}{A model formula for the mean equation. In the current workflow,
this is expected to be \code{precipitation ~ trend:factor(county_fips)}.}

\item{model}{Character string specifying the \code{plm} estimator. Common values
include \code{"within"}, \code{"pooling"}, \code{"random"}, \code{"between"}, and \code{"fd"}.}

\item{eps}{Small positive value added to squared residuals and predicted
variances to avoid taking \code{log(0)} or dividing by zero. Default is \code{1e-8}.}
}
\value{
A list containing:
\describe{
\item{mean_model_initial}{Initial fitted mean model.}
\item{variance_model}{Fitted residual-based log-variance model.}
\item{mean_model_weighted}{FGLS-style pre-weighted fitted mean model.}
\item{data}{Estimation sample used for the variance model.}
\item{data_weighted}{Pre-weighted estimation sample used for the final mean model.}
}
Returns \code{NULL} if the state does not have enough panel units or time periods,
or if any model estimation step fails.
}
\description{
Estimates a panel-data mean model, uses the residuals from that model to
estimate a variance model, constructs inverse-variance weights, and then
re-estimates the mean model after applying the weights directly to the data.
}
\details{
The workflow proceeds in six steps:

\enumerate{
\item Estimate an initial panel mean model.
\item Extract residuals from the initial mean model.
\item Construct a log-squared residual outcome:
\code{log_resid_sq = log(residual^2 + eps)}.
\item Estimate a variance equation using the same RHS variables as the
mean equation.
\item Predict the conditional variance and construct inverse-variance
weights.
\item Multiply \code{precipitation} and \code{trend} by \code{sqrt(weight)} and re-estimate
the mean model without using the \code{weights} argument in \code{plm}.
}

This pre-weighting strategy avoids the \code{plm} limitation that robust covariance
functions such as \code{vcovDC()} and \code{vcovHC()} are not implemented for weighted
panel regressions.
}
\section{Important}{

The pre-weighting block is currently hard-coded for the model:

\code{precipitation ~ trend:factor(county_fips)}

If the outcome variable or trend variable changes, the pre-weighting block
must be updated accordingly.
}

\seealso{
Other Precipitation trend analysis: 
\code{\link{classify_mean_variance_trends}()},
\code{\link{classify_trend}()},
\code{\link{estimate_by_estimator}()},
\code{\link{estimate_one_state}()},
\code{\link{extract_model_diagnostics}()},
\code{\link{fisher_joint_pvalue}()}
}
\concept{Precipitation trend analysis}
