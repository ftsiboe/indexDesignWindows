% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_turning_point.R
\name{estimate_final_turning_point}
\alias{estimate_final_turning_point}
\title{Estimate Final Turning Points Using a Data-Driven Tolerance}
\usage{
estimate_final_turning_point(
  data,
  outcome,
  group_var,
  x_var,
  benchmark_x = NULL,
  tolerance_grid = seq(0.01, 0.15, by = 0.005),
  consecutive_years = 5,
  penalty_weight = 0.01,
  require_consecutive_x = TRUE
)
}
\arguments{
\item{data}{A data.frame or data.table containing the outcome variable,
grouping variable, and historical-window variable.}

\item{outcome}{Character string. Name of the outcome variable to evaluate.
Examples include \code{"actuarial_index"}, \code{"alternative_lr"}, or
\code{"baseline_lr"}.}

\item{group_var}{Character string. Name of the grouping variable used to
estimate separate turning points. For example, \code{"adjustment"}.}

\item{x_var}{Character string. Name of the historical-window variable. This
variable should represent the number of years in the historical window.}

\item{benchmark_x}{Numeric. The x-value used as the benchmark. If \code{NULL}, the
maximum observed value of \code{x_var} is used. For example, use \code{60} for a
60-year benchmark or \code{200} if the full sample is coded as 200.}

\item{tolerance_grid}{Numeric vector. Candidate tolerance shares to evaluate.
The default evaluates values from 1 percent to 15 percent in increments of
0.5 percentage points.}

\item{consecutive_years}{Integer. Number of consecutive historical-window
values that must remain within the benchmark-distance tolerance before the
first x-value is classified as the turning point. The default is \code{5}.}

\item{penalty_weight}{Numeric. Penalty applied to later turning points in the
score function. Larger values favor earlier turning points, while smaller
values allow later turning points if they produce lower
post-turning-point variability. The default is \code{0.01}.}

\item{require_consecutive_x}{Logical. If \code{TRUE}, the function requires the
selected sequence to be consecutive in terms of the actual \code{x_var} values.}
}
\value{
A data.table with one row per group. The output includes the grouping
variable, outcome name, benchmark year, selected tolerance share, required
number of consecutive years, final turning point, post-turning-point
stability statistics, scaled turning point, and minimized score.
}
\description{
This function runs the full benchmark-distance turning-point pipeline. It
first selects the best tolerance value for each group using
\code{select_tolerance_by_post_stability()}. It then re-estimates the final
turning point for each group using the selected tolerance.
}
\details{
The function returns both the final turning point and the diagnostics used to
select the tolerance. This makes the final output easier to interpret and
audit because each reported turning point is linked to the selected tolerance,
post-turning-point stability statistics, and final score.

The full pipeline is:
\enumerate{
\item For each tolerance in \code{tolerance_grid}, estimate the earliest benchmark-
stable historical window for each group.
\item For each candidate tolerance and group, calculate the stability of the
outcome after the estimated turning point.
\item Select the tolerance that minimizes:
}

\deqn{
  score = post\_cv + penalty\_weight \times scaled\_turning\_point
}
\enumerate{
\item Re-estimate the final turning point using the selected group-specific
tolerance.
}

This function is useful when the analyst does not want to impose an arbitrary
tolerance, such as 5 percent, and instead wants the tolerance to be selected
from the data based on post-turning-point stability.
}
